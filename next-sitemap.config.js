/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://sikhsoorme.com',
    generateRobotsTxt: true,
    sitemapSize: 7000,
    outDir: './out',
};

export default config;
  